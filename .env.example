# Copy to .env.local for local development. Do not commit .env or .env.local.

# Delay in ms between each email send (3–5 seconds recommended)
EMAIL_DELAY_MS=5000

# Mailgun (required for send-emails Netlify Function)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# Cloudflare Turnstile (spam protection)
# Client-side Turnstile widget (form page). Leave empty in dev to use Cloudflare test key (localhost).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
# Cloudflare Turnstile (server-side verification). Use 1x0000000000000000000000000000000AA in dev for test key.
TURNSTILE_SECRET_KEY=
